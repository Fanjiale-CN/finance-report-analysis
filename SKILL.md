---
name: finance-report-analysis
description: "Use this skill when the user provides a financial report (10-K, 10-Q, or a Chinese A-share/HK-listed annual or quarterly report) as a PDF and wants key numbers extracted and analyzed. Supports English, Simplified Chinese, and Traditional Chinese reports. Covers the three core financial statements (income statement, balance sheet, cash flow statement) plus bank-specific regulatory ratios (NIM, NPL ratio, provision coverage, capital adequacy) when the filer is a bank. Does NOT fetch reports from the web — the user must supply the PDF. Trigger on requests like '分析这份财报' '帮我看一下这份10-K' 'extract the numbers from this annual report' or when a 10-K/10-Q/年报/季报 PDF is attached with an analysis request."
license: MIT
---

# 财报提取与分析 Skill v0.4 — Universal Cross-Market Mode

## 现状与边界（务必先读）

这是一个**全能入口 + 专属解析器 + 统一标准化 + 跨市场比较**的 skill。当前已在 A 股、港股和美股真实报告上完成回归；统一入口返回 `FinancialReport` 契约、完整性摘要和结构化问题，而不是让缺失数据以裸 `KeyError` 失败。仍需对新公司、新版式进行抽查，但已具备跨市场通用入口。
- **普通单报告分析**做同比增速和方向性描述；`scripts/compare.py` 新增跨报告比较，支持字段标准化、单位换算、汇率注入和口径警示，但不做估值、不做"好/坏"判断，也不会替代会计重述。
- **依赖版本已升级**：代码从弃用的 `fitz` 导入改为 `import pymupdf`（PyMuPDF >= 1.24）。
   旧环境装了 PyMuPDF < 1.24 时请先 `pip install -r requirements.txt` 升级。
- `scripts/universal.py` 提供 `report_type="auto"`：自动识别 A股、港股、美股以及中英文报告并路由。结果统一为 `FinancialReport`，包含 `statements`、`completeness`、`issues` 和原始 `extraction`；新公司的非标准格式仍可能需要补充关键词，低置信度必须人工确认。
- `scripts/table_reconstruction.py` 使用 PyMuPDF `words` 坐标按行聚合，作为复杂版式、标题与数据跨页或阅读顺序异常时的 fallback；它不会覆盖已通过回归的传统文本解析结果。
- `hk_quarterly` 只覆盖港股业绩公告中实际出现的利润表；完整中期/年度报告会自动路由到对应三大报表解析器。若报告同时存在多套语言或多套单位，统一层会保留口径警示。
- **未知 report_type 会直接报错**（`ValueError`），不会静默跑下去；但已知类型
  遇到报告格式变体（科目名措辞不同）时某个字段会静默缺失——新增报告类型接入时
  务必逐项抽查一遍。

## 使用流程

### 全能自动模式（推荐）

```python
from scripts.universal import extract_universal
result = extract_universal("path/to/report.pdf", report_type="auto")
print(result["detection"])  # 市场、语言、置信度、各类型得分
schema = result["schema"]
```

### 单报告标准分析

```python
# 1. 定位+提取+解析（每份报告先跑这一步，产出结构化数据）
from scripts.extract import run_extraction
result = run_extraction("path/to/report.pdf", report_type="us_10k")
# report_type 可选值见下方"支持的报告类型"

# 2. 映射到统一 schema + 计算同比 + 生成表格/文档
from scripts.analyze import build_schema, render_markdown_table, render_narrative
schema = build_schema(result, "us_10k")
table_md = render_markdown_table(schema, "us_10k", "公司名/报告期")
doc_md = render_narrative(schema, "us_10k", "公司名/报告期")
```

### 跨市场比较

```python
from scripts.compare import compare_reports, render_markdown
result = compare_reports(["byd_annual.pdf", "xiaomi_annual.pdf"], fx_to_cny={"USD": 7.2, "HKD": 0.92})
print(render_markdown(result))
```

比较层会自动识别每份报告，统一常见字段、原始单位和币种；人民币金额会转换为人民币百万元，美元/港币必须显式传入汇率，否则保留原币并标记不可比。

## 支持的报告类型 (`report_type`)

| 值 | 适用 | 覆盖报表 | 验证状态 |
|---|---|---|---|
| `us_10k` | 美股年报 | 利润表/资产负债表/现金流量表 | ✅ 逐项核对过 |
| `us_10q` | 美股季报 | 同上（4期数据：本季/去年同季/今年累计/去年累计） | ✅ 逐项核对过 |
| `cn_a_share_annual` | A股银行年报 | 利润表/资产负债表/现金流量表/银行关键指标表 | ✅ 逐项核对过 |
| `hk_quarterly` | 港股季报（简明合并报表格式，繁中） | 仅利润表 | ✅ 逐项核对过 |
| `hk_annual` | 港股年报（英文，三大报表） | 利润表/资产负债表/现金流量表 | ✅ 逐项核对过（小米 2025 年报） |
| `cn_a_share_general` | A股非银行公司年报/季报 | 利润表/资产负债表/现金流量表 | ✅ 逐项核对过（比亚迪 2025 年报） |

新增报告类型：在 `scripts/extract.py` 的 `REPORT_CONFIGS` 里加一段配置（关键词 +
每行数据的期数），先用 `find_pages()` 确认关键词能唯一定位到正文页（不是目录页、
不是附注里的引用提及——这是最容易踩的坑，见下）。

## 已知的坑（都是真实踩过的，别重复踩）

1. **中文 PDF 不能用 `pdftotext`/poppler 提取**，很多用 InDesign 排版的年报（比如
   工行、小米）用 CID 字体嵌入中文，poppler 认不出来，只会吐出数字丢光中文。必须用
   `PyMuPDF (fitz)` 的 `get_text()`，它能正确处理。
2. **关键词可能命中目录页**，而不是报表正文页——目录页里也会印着关键词和一堆页码
   数字。`extract.py` 用"数字密度"启发式挑正文页，但这只是启发式，新报告类型接入
   时务必人工抽查一次挑中的页码对不对。
3. **同一关键词在文档里到处出现**（比如"不良贷款率"会出现在分地域明细表、减值准备
   变动表等好几个地方），单关键词定位会挑到错误的页。这种情况下用多关键词共现
   （`all_keywords`，见 `key_ratios` 配置）来唯一定位。
4. **报表可能跨2页**（资产负债表常见"资产"一页、"负债+权益"另一页；现金流量表
   也可能跨页），需要设置 `span_pages`，且起始页要选跨页表格里"最靠前"的那一页，
   不能简单取数字密度最高的单页（那样会跳过前一页）。
5. **多行标签会被拆成两条数据**：中文报表里科目名换行时（如"以公允价值计量且其变动
   /计入当期损益的金融投资"），必须把连续的非数字行拼成一个 label，再去接后面的
   数字，否则标签会被截断成半句话。
6. **"–"（长破折号）在报表里表示"该期为零/不适用"**，不是噪音字符，必须当成合法的
   零值 token 处理，否则会把这一行的4个数字切成两截。
7. **字段映射子串冲突**：比如"资本充足率"是"核心一级资本充足率"的子串，两个字段
   如果都用简单的 `in` 判断会抢到同一行数据。`analyze.py` 里按 needle 长度从长到短
   匹配、且已认领的行不能被别的字段复用，来解决这个问题——新增映射字段时留意
   会不会引入新的子串冲突。
8. **同一概念在不同报告间的措辞会变**：Apple 10-K 写"Cash generated by investing
   activities"，10-Q 写"Cash generated by/(used in) investing activities"——同一
   家公司、同一类科目，季报和年报的措辞都可能不完全一样，映射关键词要么用更短的
   稳定子串，要么显式列出变体。
9. **同一报告内多量级/多单位列示**：港股业绩公告里摘要页以"百万元"列示、正文报表
   以"千元"列示，EPS 以"元/股"列示，混进同一个 values 体系前必须先确认单位，否则
   同比计算会把相差 1000 倍的数放在一起。提取阶段不自动换算单位，靠调用方人工核对。
10. **跨行报表标题会合并进第一行数据**：港股年报中"CONSOLIDATED INCOME\nSTATEMENT"
    跨行时，`find_pages()` 需要按相邻行拼接判断关键词共现（`_find_page_by_all_keywords`
    的跨行逻辑）；否则标题跨行页会漏掉，或起始页被推到下一页（现金流量表经营段
    会被整个跳过）。新类型接入时必须验证跨行标题能否命中。
11. **页头单位行污染数据行**：港股年报页头"RMB'000"单位行容易和第一行数据合并成
    "RMB'000RMB'000Revenue5"，必须加专用 filter：单位混并行直接丢弃、单位混并首
    数据行剥掉前缀单位再解析。
12. **繁中/简中/英文双 needle**：港股繁中公告（"期內利潤""銷售成本"）与英文年报
    （"Profit for the year""Cost of sales"）标签语言完全不同，`analyze.py` 里
    `hk_quarterly` 与 `hk_annual` 用两套 needle map，不能共用同一套。
13. **资本开支符号差异**：10-K 里 capex 为负值（流出），A股报表为正值（列示为"支付的
现金"）。计算 capex 同比必须统一用绝对值，否则 97亿→157亿会被算成"减少 61%"。
14. **跨市场比较不是简单拼表**：必须同时处理币种、单位、财年/季度期间、合并范围和会计准则。`compare.py` 对缺失汇率不静默换算，并在输出中保留警示；统一字段缺失也会被保留而不是填零。

## 目录结构

```
finance-report-skill/
  SKILL.md              本文件
  scripts/
    statement_parser.py 通用"标签+N期数字"逐行解析器（parse_statement）
    extract.py           定位关键页 + 调用parser + 生成结构化JSON（run_extraction）
    analyze.py            schema映射 + 同比计算 + 表格/文档输出
    universal.py           自动识别市场/语言/报告类型的全能入口
    compare.py             跨市场字段、单位、币种、准则警示与比较输出
    table_reconstruction.py 基于 words 坐标的表格行重建 fallback
```

## 下一步（已完成项与后续迭代）

已完成：A股、港股、美股自动识别与路由；中国电信、HKEX、Disney、小米、比亚迪五份真实样本三大报表回归通过；复杂标题大小写、日期顺序、中文报表简称和跨页表格兼容；统一 `FinancialReport` 契约；跨市场金额标准化、会计准则识别与缺汇率/低置信度/不完整报告警示。

后续迭代：

1. 硬件类公司的分部收入/分部毛利率（嵌套结构，比三大报表复杂，还没设计）
2. 银行的杜邦分解（ROE = ROA × 权益乘数）等更深的分析逻辑
3. A股非银行公司的衍生指标补全（ROE、资产负债率、扣非净利润——这些字段在
   非银行年报的关键指标区里，但格式不统一，还没设计通用抓取）
4. 港股年报的繁中/英文双语言报告支持（当前 `hk_annual` 只支持英文年报，
   繁中年度综合报告需要另一套 needle）
